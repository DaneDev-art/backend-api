# Configuration du serveur
PORT=5000

# Connexion MongoDB
# Utilise 127.0.0.1 pour éviter les problèmes IPv6 (::1)
MONGO_URI=mongodb://127.0.0.1:27017/auth_db

# Secret pour signer les tokens JWT
JWT_SECRET=supersecretkey123
